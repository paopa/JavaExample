plugins {
    id 'java'
}

group 'per.pao.practice'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':api')
    runtimeOnly project(':impl')
}

test {
    useJUnitPlatform()
}